#include "main.h"
/**
 * _abs - a C program that computes the absolute value of an integer.
 * @a: the integer to be computed
 * Return: a (if positive), -a (if negative)
 **/
int _abs(int a)
{
	if (a >= 0)
	{
		return (a);
	}
	else
	{
		return (-a);
	}
}
